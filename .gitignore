.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.devin/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# User-specific configuration and runtime state.
config/secrets.env
config/secrets-*.env
config/*.token
config/*.yaml
systemd/*.service
sessions/
sessions-*/
sessions.jsonl
dispatch_store.jsonl
wake_queue.jsonl
wake_queue.jsonl.lock
runtime-overrides.yaml
/plans/
/plans.jsonl
plugin-incidents.jsonl

# Probe output artifacts.
probes/*.log
probes/*.json
probes/*.txt

# Misc.
*.log
.env
.coverage

# uv lockfile (generated from pyproject.toml).
uv.lock

# Plan review drafts — session-specific review notes, not committed.
PLAN-REVIEW-*.md
